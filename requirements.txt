aiogram>=3.7,<4
vkbottle>=4.3,<5
aiohttp>=3.9
python-dotenv>=1.0
Pillow>=10.0
certifi>=2023.0
yt-dlp>=2024.0
discord.py>=2.4,<3
# Распознавание речи голосовых (offline). Нужны ещё ffmpeg (системный) и модель
# Vosk (см. .env.example, VOSK_MODEL_PATH). Если не установлено — фича просто
# отключается, мост работает.
vosk>=0.3.45
